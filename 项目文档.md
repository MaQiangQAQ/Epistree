# Epistree Demo — 项目文档

> 最后更新：2026-09-08

## 项目概览

知乎黑客松项目「Epistree：知识世界树」。从知乎来源中抽取结构化知识并生成可交互的世界树图。

## 目录结构

```
.
├── pyproject.toml              # 项目配置与依赖
├── .env.example                # 环境变量模板
├── .gitignore
├── Dockerfile                  # 容器化部署
├── README.md
├── CHANGELOG.md
├── ROADMAP.md
├── docs/
│   ├── DEMO_DESIGN.md          # Demo 设计文档（实现依据）
│   └── ENGINEERING_DESIGN.md   # 工程设计文档
├── src/epistree_demo/
│   ├── __init__.py
│   ├── app.py                  # Dash 主应用
│   ├── config.py               # Pydantic Settings
│   ├── models.py               # Pydantic 数据契约 + FlatGraphBundle
│   ├── db.py                   # SQLite 建表与操作
│   ├── query_builder.py        # 固定规则查询生成
│   ├── zhihu_client.py         # 知乎 HTTP API + requests-cache
│   ├── selector.py             # 去重与轮询入选
│   ├── extractor.py            # Instructor 结构化抽取
│   ├── presenter.py            # Cytoscape 元素转换
│   └── service.py              # 主链路编排
├── tests/
│   ├── __init__.py
│   ├── fixtures/
│   │   └── zhihu_search.json   # 测试夹具（源自真实响应）
│   ├── test_models.py          # 模型校验测试
│   ├── test_query_builder.py   # 查询生成测试
│   ├── test_selector.py        # 去重与选择测试
│   ├── test_presenter.py       # 图元素转换测试
│   └── test_demo_flow.py       # 端到端流程测试
└── .venv/                      # Python 3.14 虚拟环境
```

## 当前状态

**已完成：**
- Step 1 离线骨架：所有模块文件、37 个 pytest 全部通过
- Step 4 结构化抽取：FlatGraphBundle → GraphBundle 转换通过真实 API 验证
- Step 5 交互图：Dash Cytoscape 节点/边样式、点击详情、JSON 导出

**进行中：**
- Step 2 知乎 Client 与 HTTP 缓存测试
- Step 3 去重/快照/预算完整管线
- Step 6 部署（Dockerfile + 预热主题）

## 关键决策

### 模型抽取策略
模型输出扁平 `nodes + relations`（FlatGraphBundle），再转换为规范的 `questions/claims/events`（GraphBundle）。避免强迫模型提前分类。

### 模型选择
使用 `deepseek-v4-flash`（第三方 API，OpenAI 兼容）。稳定输出 claim，不自动生成 question（代码补全），短文本下不生成事件/关系。

## 运行方式

```bash
# 首次
uv sync --python 3.14

# 开发运行（离线静态演示）
CD DATA_DIR=./.data PYTHONPATH=src uv run python -m epistree_demo.app

# 测试
uv run --with-editable . pytest tests/ -v
```

## 环境变量

参见 `.env.example`。注意 `PYTHONPATH` 会被 Hermes 会话污染，运行时须加 `env -u PYTHONPATH`。
