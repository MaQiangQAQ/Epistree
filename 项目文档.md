# Epistree Demo — 项目文档

> 当前版本：v0.2.0<br>
> 最后更新：2026-09-10

## 项目概览

知乎黑客松项目「Epistree：知识世界树」。项目从知乎来源中抽取结构化知识，生成可交互、可回溯、可按时间探索的世界树。当前 Demo 已具备离线预热浏览、自定义主题查询、缓存与配额治理、来源约束、节点聚焦、年份筛选、知识生长回放和 JSON 导出。

## 当前运行边界

- 三个预热主题从 `DATA_DIR` 中的快照读取，不消耗知乎或模型额度；`.data` 不由 Git 跟踪，部署时需单独挂载。
- 自定义主题仅使用知乎开放数据，真实网络请求同时受官方剩余额度和本地 SQLite 日预算约束。
- 图中的自动关系均为带置信度与来源的 `model_candidate`，不作为事实判定。
- 离线测试与本地 Dash 页面已验收；真实知乎接口契约和 Docker 构建仍待对应环境验证。

## 目录结构

```text
.
├── README.md                       # 使用入口与能力边界
├── CHANGELOG.md                    # 逐版本变更记录
├── VERSION                         # 当前版本号
├── ROADMAP.md                      # 迭代路线
├── docs/
│   ├── DEMO_DESIGN.md              # Demo 实现基线
│   ├── DEMO_REMEDIATION_PLAN.md    # 修复实施记录与验收边界
│   ├── EPISTREE_FRONTEND_DESIGN.md # 前端视觉与交互设计
│   └── ENGINEERING_DESIGN.md       # 完整工程设计
├── src/epistree_demo/
│   ├── app.py                      # Dash 页面与交互回调
│   ├── config.py                   # 配置与启动边界
│   ├── models.py                   # 数据契约与来源约束
│   ├── db.py                       # SQLite 存储、预算和运行状态
│   ├── zhihu_client.py             # 知乎接口、缓存、重试和额度检查
│   ├── extractor.py                # 结构化模型抽取
│   ├── presenter.py                # Cytoscape 数据与确定性布局
│   ├── service.py                  # 主链路编排
│   ├── warmup.py                   # 预热快照加载
│   └── assets/                     # 样式、粒子与键盘增强
└── tests/                          # 离线单元与回归测试
```

## 关键设计决定

模型输出扁平的 `nodes + relations`，再转换为规范图谱；节点和关系必须引用本次入选的知乎来源。知乎搜索先查持久缓存，缓存未命中才预留本地日预算并请求上游；过期响应只在限定宽限期和上游异常时降级使用。界面采用 Dash 与 Dash Cytoscape，不自研渲染器，使用纵向时间、横向观点分叉的预设布局。

## 运行与验证

```bash
uv sync --frozen
uv run epistree-demo
uv run --python 3.14 pytest
```

浏览器访问 `http://localhost:8050`。环境变量与部署方式见 [README.md](README.md)，实现依据和未验证边界见 [docs/DEMO_DESIGN.md](docs/DEMO_DESIGN.md) 与 [docs/DEMO_REMEDIATION_PLAN.md](docs/DEMO_REMEDIATION_PLAN.md)。
